<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logonotas"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notas"
        tools:targetApi="31">
        <activity
            android:name=".EditarEliminarDiario"
            android:exported="false"
            android:theme="@style/colordelmenu"/>
        <activity
            android:name=".anadirdiario"
            android:exported="false"
            android:theme="@style/colordelmenu" />
        <activity
            android:name=".DiarioMain"
            android:exported="false"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditarEliminarNota"
            android:theme="@style/colordelmenu" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".anadirnota"
            android:theme="@style/colordelmenu" />
    </application>

</manifest>